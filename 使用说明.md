# 使用说明 · 像素主页 v3

整页是一张「游戏存档」：浅色模式是白天的南京，深色模式是夜晚的南京。

| 区块 | 文件 | 内容 |
| :-- | :-- | :-- |
| 标题画面 | `assets/hero-*.svg` | 紫金山 + 天文台、紫峰大厦、明城墙和城楼；白天飘梅花、飞鸟，夜晚有星星、流星和亮灯的窗户；城墙上一只在敲电脑的猫 |
| 对话框 | `assets/dialog-*.svg` | RPG 对话框，逐字打出自我介绍 |
| 能力面板 | `assets/status-*.svg` | 六边形雷达图 + 6 项能力（等级、关键词） |
| 任务日志 | `assets/quest-*.svg` | 4 个主线任务（进行中）+ 8 个支线任务（已通关），卡片右上角标注它锻炼了哪项能力 |
| 装备栏 | `assets/inventory-*.svg` | 技术栈，金色星标 = 主力装备 |
| 成就 | `assets/achievements-*.svg` | 奖项、里程碑 |
| 存档 | `output` 分支 | 每天由 Actions 自动生成的像素统计卡 + 贪吃蛇 |
| 结尾 | `assets/footer-*.svg`、`assets/btn-*.svg` | THANKS FOR PLAYING + 邮件 / 仓库按钮 |

每个组件都有 `-light` / `-dark` 两份。默认所有访客都看夜晚版；想跟随访客系统深浅色自动切换，把 `profile.toml` 里的 `theme` 改成 `"auto"`（或 `"light"` 固定白天版）。
所有文字都已经画成像素方块，SVG 里没有字体、脚本或外链，在 GitHub 的图片沙箱里也能原样显示。

## 修改内容

所有文字都在 **`tools/profile.toml`**。改完运行：

```bash
python3 tools/build.py      # Python 3.11+，只用标准库，不需要 pip install
```

它会重新生成 `assets/` 里的全部 SVG 和 `README.md`（README 是生成的，不要手改）。
文字太长放不进卡片时，build 会直接报错并告诉你是哪一行，不会悄悄溢出。

常见改法：

- **调能力等级**：改 `[[ability]]` 里的 `level`（1–10），雷达图和进度条会一起变。
- **加 / 换项目**：复制一段 `[[quest]]`。`kind = "main"` 是主线（进行中），`"side"` 是支线；
  `skills` 填能力代号（AGT / LLM / WEB / SCI / CV / OPS），卡片颜色跟第一个能力走；
  `icon` 可选：sigma heart shield gem bolt pencil circuit scroll chart trophy light note clock star rocket chest。
  主线卡片的英文简介最多两行，支线一行。
- **更多项目**：`[[more]]` 会进折叠列表，只是文字，不限长度。
- **装备栏**：`items` 里名字后面加 `*` 就会带金色星标。
- **对话**：`[dialog]` 的 `lines`，每行一句。

## 自动更新（`.github/workflows/profile-data.yml`）

每天北京时间 02:00 和每次 push 到 main 时运行，产物推到 `output` 分支：

- `save-data-light/dark.svg`：像素统计卡（星标、仓库、关注者、年度贡献、连续天数、常用语言），
  由 `tools/stats.py` 调 GitHub GraphQL 生成；接口偶尔失败时会沿用上一次的卡片。
- `snake-pixel-light/dark.svg`：配色和主页统一的贪吃蛇。
- `github-contribution-grid-snake*.svg`：v2 用的旧贪吃蛇，留着方便回滚。

合并到 main 之后第一次运行前（大约一分钟），存档卡片和贪吃蛇会暂时显示不出来，跑完刷新即可；
也可以在 Actions 页面手动 Run workflow。本地预览统计卡：`python3 tools/stats.py --mock --out /tmp/preview`（假数据，只看排版）。

## 回滚

旧版 v2 完整保存在 [`versions/v2-macaron/`](versions/v2-macaron/)，回滚命令见 [`versions/README.md`](versions/README.md)。

## 文件结构

```
README.md                      生成的主页（别手改）
assets/*-light.svg / -dark.svg 生成的像素组件
tools/
  profile.toml                 ← 所有文字内容，改这里
  build.py                     生成 assets/ 和 README.md
  scene.py                     标题画面（天空、紫金山、紫峰大厦、城墙、猫）
  cards.py                     对话框、能力面板、任务卡、装备栏、成就、结尾
  ui.py                        窗口、标题栏、标签、进度条、像素图标
  pixel.py                     位图字体 → SVG 像素方块的渲染器
  theme.py                     浅色 / 深色两套配色
  stats.py                     Actions 里生成存档统计卡
  extract_font.py              从字体文件提取新字符（一般用不到）
  fonts/                       位图字体数据 + OFL 许可证
versions/                      历史版本存档
.github/workflows/profile-data.yml
```

## 字体与许可

像素字形提取自以下字体（均为 SIL Open Font License 1.1，许可证在 `tools/fonts/`）：

- 正文（中英文）：Fusion Pixel Font 12px / Ark Pixel Font（TakWolf），收录 GB2312 全部 6763 个汉字
- 标题：Press Start 2P（数据文件按 OFL 要求改名为 `arcade-8px`）
- 小号标签：Tiny5

遇到生僻字 build 会提示缺字。补字方法：从 <https://fusion-pixel-font.takwolf.com/> 下载
`fusion-pixel-12px-proportional-zh_hans.otf.woff2`，然后：

```bash
pip install fonttools brotli
python3 tools/extract_font.py fusion-pixel-12px-proportional-zh_hans.otf.woff2 \
    tools/fonts/pixel-12px.txt --px 12 --gb2312-full --extra "要加的字"
```

## 设计备忘

- 像素网格：场景 1 像素 = 4 个 SVG 单位，正文字号 2 倍；`shape-rendering="crispEdges"` 保证边缘锐利。
- 配色延续 v2 的马卡龙八色，夜间版提亮成霓虹色，见 `tools/theme.py`。
- 动画都按整像素步进（`steps()`），并支持 `prefers-reduced-motion`（系统开启「减少动态效果」时全部静止，文字照常显示）。
